@page
@model LogoutModel
@{
    ViewData["Title"] = "РР·С…РѕРґ";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-12 col-md-7 col-lg-5">
            <div class="card shadow-sm border-0">
                <div class="card-body p-4 text-center">

                    <h1 class="h4 mb-3">@ViewData["Title"]</h1>

                    @if (User.Identity?.IsAuthenticated ?? false)
                    {
                        <p class="text-muted mb-4">
                            РЎРёРіСѓСЂРµРЅ Р»Рё СЃРё, С‡Рµ РёСЃРєР°С€ РґР° РёР·Р»РµР·РµС€ РѕС‚ РїСЂРѕС„РёР»Р° СЃРё?
                        </p>

                        <form asp-area="Identity"
                              asp-page="/Account/Logout"
                              asp-route-returnUrl="@Url.Page("/", new { area = "" })"
                              method="post">

                            <button type="submit" class="btn btn-danger w-100">
                                рџљЄ РР·С…РѕРґ
                            </button>
                        </form>
                    }
                    else
                    {
                        <div class="alert alert-success mb-0">
                            РЈСЃРїРµС€РЅРѕ РёР·Р»РµР·Рµ РѕС‚ РїСЂРѕС„РёР»Р° СЃРё.
                        </div>

                        <a asp-page="/Index" class="btn btn-outline-primary mt-3 w-100">
                            в¬…пёЏ РќР°С‡Р°Р»РЅР° СЃС‚СЂР°РЅРёС†Р°
                        </a>
                    }

                </div>
            </div>
        </div>
    </div>
</div>
